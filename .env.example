# MedicalDocBot — Environment Variables
# Sao chép file này thành .env và điền giá trị thực
# TUYỆT ĐỐI không commit file .env lên git

# ── Telegram Bot ──────────────────────────────────────────────────────────────
# Lấy token từ @BotFather trên Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Danh sách Telegram user ID được phép dùng bot (phân cách bằng dấu phẩy)
# Lấy user ID bằng cách nhắn tin cho @userinfobot
TELEGRAM_ALLOWED_USERS=123456789,987654321

# ── Đường dẫn dữ liệu ─────────────────────────────────────────────────────────
# Thư mục gốc lưu tài liệu thiết bị y tế (WHITELIST PATH)
MEDICAL_DEVICES_DIR=/Users/xitrum/MedicalDevices

# ── Database ──────────────────────────────────────────────────────────────────
# Đường dẫn SQLite database (mặc định: trong MEDICAL_DEVICES_DIR)
# DB_PATH=/Users/xitrum/MedicalDevices/.db/medicalbot.db

# ── Logging ───────────────────────────────────────────────────────────────────
# Mức log: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ── Watcher ───────────────────────────────────────────────────────────────────
# Thời gian debounce (giây) trước khi xử lý file event
WATCHER_DEBOUNCE_SECONDS=3

# ── Extractor ─────────────────────────────────────────────────────────────────
# Thư mục cache kết quả extract (mặc định: MEDICAL_DEVICES_DIR/.cache/extracted)
# EXTRACT_CACHE_DIR=/Users/xitrum/MedicalDevices/.cache/extracted
