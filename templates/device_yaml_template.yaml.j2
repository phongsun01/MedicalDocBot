---
vendor: "{{ vendor }}"
model: "{{ model }}"
category_id: "{{ category_id }}"
category_slug: "{{ category_slug }}"
risk_class: "{{ risk_class | default('C') }}"
year: {{ year | default(2020) }}
hs_code: "{{ hs_code | default('') }}"
status: "{{ status | default('Hoạt động') }}"
power_kw: {{ power_kw | default(0) }}
weight_kg: {{ weight_kg | default(0) }}
aliases: {{ aliases | default([]) | tojson }}
links:
  fda: "{{ links.fda | default('') }}"
  ce: "{{ links.ce | default('') }}"
files:
  ky_thuat: []
  cau_hinh: []
  bao_gia: []
  trung_thau: []
  hop_dong: []
  so_sanh: []
  khac: []
created_at: "{{ created_at }}"
updated_at: "{{ updated_at }}"
